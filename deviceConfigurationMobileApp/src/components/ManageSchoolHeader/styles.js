import { StyleSheet } from 'react-native';
import { Mixins } from '../../config/styles';

const styles = StyleSheet.create({
  containerStyle: {
    alignSelf: 'center',
    justifyContent: 'center',
    marginEnd: Mixins.scaleSize(18)
  },
});
export default styles;
